# jQuery EncircleText.js

This simple plugin can create encircle text within textbox value. Only work for _textbox_ and _textarea_.

How to use:

`$(SELECTOR).encircleText(STRING)`


Example:

`$('input').encircleText('*')`
